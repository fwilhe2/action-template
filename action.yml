name: 'my-action'
description: 'Provide a description here'
author: 'Florian Wilhelm'
inputs:
  guest_name: # change this
    required: true
    description: 'What is the name of our guest?'
    default: 'Michael'
runs:
  using: 'node20'
  main: 'dist/index.js'
